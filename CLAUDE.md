# CLAUDE.md

이 저장소에서 작업하는 모든 사람(사람이든 AI든)이 따르는 규칙이다.
작업을 시작하기 전에 이 문서를 먼저 읽는다.

---

## 프로젝트

**Samantha** — 나를 기억하고, 시간이 지날수록 관계가 깊어지는 AI Companion 앱.
iOS / Android 상용 서비스. 한국 출시 후 해외 확장을 고려한다.

- 현재 단계: **Phase 1 — 프로젝트 기반 설계** (앱 코드 없음)
- 진행 상황은 `README.md` 의 Phase 표를 본다.

---

## 프로젝트의 성격

**Samantha는 개인 연습용이나 프로토타입으로 끝나는 프로젝트가 아니다.**
실제 사용자가 가입하고 결제하는 상용 서비스를 목표로 한다.

따라서 처음부터 다음을 고려한다.

- 보안
- 개인정보 보호
- 안정성
- 테스트 가능성
- 유지보수성
- 확장성
- 장애 대응
- 데이터 무결성

**다만 과잉 설계는 하지 않는다.**
미래의 대규모 트래픽을 예상해서 현재 필요하지 않은 복잡한 시스템을 미리 구축하지 않는다.
지금 필요한 것을, 나중에 확장할 수 있는 형태로 만든다.

### 저장소 자립성

**이 프로젝트는 특정 Claude 대화나 특정 개발자에게 종속되어서는 안 된다.**

다른 컴퓨터의 Claude Code 또는 새로운 개발자가 GitHub 저장소만 전달받아도
다음을 이해할 수 있어야 한다.

- 현재 상태
- 지금까지의 결정 사항과 그 이유
- 실행 방법
- 다음에 해야 할 작업

따라서 **중요한 정보는 반드시 저장소의 문서와 코드에 남긴다.**
대화 안에서만 오간 결정은 존재하지 않는 결정으로 취급한다.

---

## 읽는 순서

1. `README.md` — 현재 상태
2. `docs/PRODUCT.md` — 무엇을 만드는가 (**모든 판단의 기준**)
3. `docs/ARCHITECTURE.md` — 어떻게 만드는가
4. `docs/DECISIONS/` — 왜 그렇게 정했는가

---

## 함께 일하는 사람

저장소 소유자는 **개발 경험이 거의 없다.** 따라서:

- 기술 용어는 처음 나올 때 풀어서 설명한다.
- **기술적으로 잘못된 요청이 오면 그대로 구현하지 않는다.**
  문제점과 더 안전한 대안을 먼저 설명하고, 판단을 받는다.
- 중요한 기술 결정을 임의로 내리지 않는다.
  **선택지와 장단점을 제시하고, 추천안을 밝힌 뒤 결정을 받는다.**
- 결정된 사항은 `docs/DECISIONS/` 에 기록한다.

---

## 실행 규칙

- **파일 생성/수정/삭제, 커밋, 설치, 외부 업로드는 승인을 받고 실행한다.**
  파일 읽기와 확인은 승인 없이 해도 된다.
- 한 번에 여러 개를 만들지 않는다. **한 단계씩 만들고, 확인받고, 다음으로 간다.**
- 기존 기능을 수정할 때는 **영향 범위를 먼저 확인하고 보고한다.**
- 요청받지 않은 기능을 추가하지 않는다.

---

## 절대 규칙 (예외 없음)

1. **API Key와 Secret을 클라이언트 코드나 Git 저장소에 두지 않는다.**
   AI 호출은 반드시 `앱 → 서버 측 API 계층 → AI 모델` 경로로만 한다.
   모바일 앱에서 AI API를 직접 호출하지 않는다.
2. **RLS(Row Level Security)가 없는 테이블을 만들지 않는다.**
3. **Supabase 대시보드에서 DB를 직접 수정하지 않는다.**
   모든 변경은 `supabase/migrations/` 의 SQL 파일로 남기고 커밋한다.
4. **Expo Go를 사용하지 않는다.** EAS Development Build 로 개발한다.
5. **모든 대화·기억·관계 데이터는 `사용자 × Companion` 단위에 귀속된다.**
   `사용자` 단위로 저장하는 스키마는 잘못된 스키마다. (`docs/PRODUCT.md` 4장)
6. **두 Companion 사이에 기억이나 관계 데이터를 공유하지 않는다.**
7. **성적으로 노골적인 콘텐츠를 생성하지 않는다.** (`docs/SAFETY.md`)
8. 실제 사용자 데이터가 있는 운영 환경에서 개발하거나 테스트하지 않는다.

---

## 기술 스택

확정된 것만 적는다. 확정되지 않은 것은 `docs/DECISIONS/` 에서 논의 후 확정한다.

| 영역 | 선택 | 상태 |
|---|---|---|
| 모바일 앱 | React Native + Expo, TypeScript (strict) | 확정 |
| 백엔드 | 서버 측 API 계층 (AI 키 및 Secret 보호) | 확정 (구현체 미정) |
| 데이터베이스 / 인증 | Supabase | 확정 |
| AI 모델 | 미정 | Phase 4 |
| 기억 시스템 구현 방식 | 미정 | Phase 5 |
| 결제 구현 방식 | 미정 | Phase 7 |

### 구조 전제

Samantha는 실제 App Store와 Google Play에 출시할 상용 모바일 앱이므로,
**프론트엔드 모바일 앱과 서버 측 백엔드 계층을 모두 구축한다.**

```
[모바일 앱]  →  [서버 측 API 계층]  →  [AI 모델]
                        ↓
                   [Supabase]
```

### 확정 사항

- 모바일 앱: React Native + Expo + TypeScript strict
- 데이터베이스 / 인증: Supabase
- 서버 측 API 계층: **반드시 사용**
- AI API를 모바일 앱에서 직접 호출하지 않음

### 미정 사항

- AI 모델
- 백엔드 구현체
- 기억 시스템 구현 방식
- 결제 구현 방식

**백엔드 구현체는 현재 단계에서 확정하지 않는다.**
Supabase Edge Functions 를 사용할지 별도의 백엔드 서버를 구축할지는,
추후 아키텍처 설계 단계에서 **보안 / 확장성 / 비용 / 유지보수성**을 비교한 뒤 결정한다.

**필요하지 않은 라이브러리를 추가하지 않는다.** 추가할 때는 이유를 남긴다.

---

## 코드 규칙

> Phase 2(앱 껍데기)에서 구체화한다. 현재 확정된 것만 적는다.

- TypeScript `strict` 모드를 사용한다. `any` 를 쓰지 않는다.
- 사용자에게 보이는 문자열을 코드에 직접 쓰지 않는다.
  다국어 확장을 위해 키로 관리한다.
- 개발 / 운영 환경을 분리한다. 환경변수 이름은 `.env.example` 에만 적는다.

---

## Git 규칙

- 브랜치: `main` 은 항상 동작하는 상태를 유지한다.
- 커밋 메시지: `종류: 내용`
  - `feat` 새 기능 / `fix` 버그 수정 / `docs` 문서 / `chore` 설정·잡일 / `refactor` 구조 개선 / `test` 테스트
- 커밋은 작게, 자주 한다.
- **`.env` 나 키가 담긴 파일은 절대 커밋하지 않는다.**

---

## 문서 관리 규칙

- **결정한 내용은 대화에 남기지 않고 문서에 남긴다.**
  이 저장소만 받아도 개발을 이어갈 수 있어야 한다.
- 제품 정책이 바뀌면 `docs/PRODUCT.md` 를 먼저 고치고, 그 다음 코드를 고친다.
- 기술 선택을 하면 `docs/DECISIONS/` 에 기록한다.
  (배경 / 선택지 / 결정 / 이유 / 되돌릴 때의 비용)
